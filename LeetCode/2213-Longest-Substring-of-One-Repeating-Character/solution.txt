                cs[i] = c;
                int node = size + i;
                lc[node] = rc[node] = c;
                for (node >>= 1; node >= 1; node >>= 1) {
                    pushUp(node);
                }
            }
            ans[q] = best[1];
        }
        return ans;
    }

    private void pushUp(int node) {
        int l = node << 1, r = l | 1;
