# 10. Regular Expression Matching

![Platform](https://img.shields.io/badge/Platform-LeetCode-blue) ![Difficulty](https://img.shields.io/badge/Difficulty-Hard-orange) ![Language](https://img.shields.io/badge/Language-class Solution {
    public boolean isMatch(String s, String p) {
        return s.matches(p);
    }
}-green)

## 🧩 Problem Summary

See the original problem on LeetCode

## 💻 Solution

```class solution {
    public boolean ismatch(string s, string p) {
        return s.matches(p);
    }
}
                }
            }
        }
      
        // Cache the result before returning
                    isMatch = matchHelper(textIndex + 1, patternIndex + 1);
                    // Characters match, move both pointers forward
                if (textChar == patternChar || patternChar == '.') {
              
                char patternChar = pattern.charAt(patternIndex);

```

## 🏷️ Tags

`LeetCode` `Coding` `class Solution {
    public boolean isMatch(String s, String p) {
        return s.matches(p);
    }
}`

## 📅 Solved On

2026-08-29

---
*Auto-pushed by [CodePush Extension](https://github.com)*
