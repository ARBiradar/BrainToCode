                break;
            }
        }
            if (num != 0) {
                allZero = false;
        for (int num : nums) {
        boolean allZero = true;


        if (allZero) {
            return 0;
        }

        // Calculate XOR of all elements
