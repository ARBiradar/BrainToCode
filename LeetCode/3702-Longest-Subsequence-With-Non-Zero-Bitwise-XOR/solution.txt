
        // If total XOR is non-zero, entire array is the longest 
        subsequence
        if (totalXor != 0) {
            return n;
        }

        // If total XOR is zero, remove one element
        return n - 1;
    }
}
