                break;
            }
        }
                allZero = false;

        if (allZero) {
            return 0;
        }

        // Calculate XOR of all elements
        int totalXor = 0;

        for (int num : nums) {
