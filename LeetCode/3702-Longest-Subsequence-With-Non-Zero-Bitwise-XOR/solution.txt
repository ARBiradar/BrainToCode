        if (allZero) {
            return 0;
        }

        // Calculate XOR of all elements
        int totalXor = 0;

        for (int num : nums) {
            totalXor ^= num;
        }

        // If total XOR is non-zero, entire array is the longest 
        subsequence
        if (totalXor != 0) {
